package com.example.android.login_impl.domain.repository

import com.example.android.login_impl.domain.model.User
import kotlinx.coroutines.flow.Flow

interface LoginRepository {
    //fun loginUser() : Flow<User>
}